import { colors } from './colors';
import { spacing } from './spacing';
import { typography } from './typography';
import { gradients } from './gradients';

export const theme = {
  colors,
  spacing,
  typography,
  border: {
    radius: {
      sm: 4,
      md: 8,
      lg: 12,
      xl: 16,
      round: 9999,
    },
  },
};

export { colors, spacing, typography, gradients };
